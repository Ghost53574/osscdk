using System;
//using CSDK.Drawing;
//using CSDK.Objects;

namespace CSDK {
    namespace Effects {
        public class Animations { /*
			private string name;
		    private Asset[] assets;
			private Metadata metadata;

		    public Animations(string name, Asset[] assets, Metadata metadata) {
			    this.name = name;
				this.assets = assets;
				this.metadata = metadata;
		    }

			public string Name {
				get { return name; }
				set { name = value; }
			}

			public Asset[] Assets {
				get { return assets; }
				set { assets = value; }
			}

			public Metadata Data {
				get { return metadata; }
				set { metadata = value; }
			}

			public Asset this[int index] {
				get { return (index <= assets.Length && index >= 0) ? assets[index] : null; }
			} */
	    }
    }
}
